# Copy this to .env and source it (or set envs in your service manager)
POS_DB_PATH=pos.db
POS_BACKUP_DIR=pos_backup

# ERPNext API (leave unset for dry-run during dev)
ERP_BASE=https://erp.yourdomain.com
ERP_API_KEY=yourkey
ERP_API_SECRET=yoursecret
